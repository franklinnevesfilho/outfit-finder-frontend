import React from 'react';
import {Screen, Text} from "@/components";

function Wishlist() {
    return (
        <Screen>
            <Text>Wishlist</Text>
        </Screen>
    );
}

export default Wishlist;